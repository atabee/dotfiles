[user]
	name = Tanaka Geruge
	email = tanakageruge@gmail.com
[core]
	editor = vim -c \"set fenc=utf-8\"
[ghq]
  root = $GOPATH/src
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
[pull]
	rebase = false
